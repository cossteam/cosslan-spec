package constant

const (
	// ToolDefaultUnixSocket Path for the unix socket.
	// daemon by default always listens on the default unix socket
	ToolDefaultUnixSocket = "/var/run/cosslan.sock"

	// ToolDefaultPort daemon listen port
	ToolDefaultPort = 6976
)
